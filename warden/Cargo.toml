[package]
name = "warden"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
landlock = "0.4.5"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"
toml = "1.1.2"
